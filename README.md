# Jenkinsfile create files and Folder

## Requirements for Nodo

- Update Ubuntu:

`sudo apt-get update`

- Install Java:

`sudo apt install openjdk-8-jre-headless`

- Install Git

`sudo apt install git`

- Install Docker: